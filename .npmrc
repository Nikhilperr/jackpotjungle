# @codetrix-studio/capacitor-google-auth peers Capacitor 6; this app is on Capacitor 8.
# Without this, npm install fails on VPS (ERESOLVE) and a partial/broken build shows a black screen.
legacy-peer-deps=true
